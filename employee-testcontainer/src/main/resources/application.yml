spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/employees}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
    platform: postgres
  jpa:
    properties:
      hibernate.temp.use_jdbc_metadata_defaults: false

